// Type inference means it will automatically understand what the type is by observing the value

let a = 20; // if we check the type of variable (a) it will show, it is number. Because it always automatically determine the type by observing data

let isActive = false; // same result goes for isActive


